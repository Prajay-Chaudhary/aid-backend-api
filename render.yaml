databases:
  - name: postgres
    ipAllowList: []
    databaseName: aid_backend_api
    user: aid_backend_api

services:
  - type: web
    name: aid_backend_api
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false